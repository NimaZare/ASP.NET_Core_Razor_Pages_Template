@page
@model Server.Pages.Admin.Users.DeleteModel

@{
	var pageTitle = $"{Resources.DataDictionary.DeleteOf} {Resources.DataDictionary.User}";
	ViewData[Constants.ViewDataKeyName.PageTitle] = pageTitle;
}

<section-form>
	<fieldset disabled>
		<section-form-header>
			@(pageTitle)
		</section-form-header>

		<partial name="PartialViews/_DisplayPageMessages" />

		<nzlib-readonly-input asp-for="ViewModel.Id" />

		<nzlib-readonly-input asp-for="ViewModel.Username" />

		<nzlib-readonly-checkbox asp-for="ViewModel.IsActive" />

		<section-form-check>
			<input asp-for="ViewModel.IsRoleActive" />
			<label asp-for="ViewModel.IsRoleActive"></label>
		</section-form-check>

		<section-form-check>
			<input asp-for="ViewModel.IsProfilePublic" />
			<label asp-for="ViewModel.IsProfilePublic"></label>
		</section-form-check>

		<nzlib-readonly-input asp-for="ViewModel.EmailAddress" />

		<nzlib-readonly-checkbox asp-for="ViewModel.IsEmailAddressVerified" />

		<nzlib-readonly-input asp-for="ViewModel.CellPhoneNumber" />

		<nzlib-readonly-checkbox asp-for="ViewModel.IsCellPhoneNumberVerified" />

		<nzlib-readonly-input asp-for="ViewModel.Ordering" />

		<nzlib-readonly-textarea asp-for="ViewModel.AdminDescription" />
	</fieldset>

	<form method="post">
		<section-form-buttons>
			<input type="hidden" asp-for="ViewModel.Id" />

			<button-delete />
		</section-form-buttons>
	</form>

	<section-form-footer>
		<a asp-page="Index">
			@(Html.NZLibGetLinkCaptionForList())
		</a>
		<a asp-page="Details" asp-route-id="@(Model.ViewModel.Id)">
			@(Html.NZLibGetLinkCaptionForDetails())
		</a>
		<a asp-page="Update" asp-route-id="@(Model.ViewModel.Id)">
			@(Html.NZLibGetLinkCaptionForUpdate())
		</a>
	</section-form-footer>
</section-form>
