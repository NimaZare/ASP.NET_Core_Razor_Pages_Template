@page
@model Server.Pages.Admin.Users.UpdateModel

@{
	var pageTitle = $"{Resources.DataDictionary.UpdateOf} {Resources.DataDictionary.User}";
	ViewData[Constants.ViewDataKeyName.PageTitle] = pageTitle;
}

<form method="post">

	<input type="hidden" asp-for="ViewModel.Id" />

	<section-form>
		<fieldset>
			<section-form-header>
				@(pageTitle)
			</section-form-header>

			<partial name="PartialViews/_DisplayPageMessages" />

			<nzlib-full-input asp-for="ViewModel.EmailAddress" dir="ltr" />

			<nzlib-full-checkbox asp-for="ViewModel.IsEmailAddressVerified" />

			<nzlib-full-input asp-for="ViewModel.Username" dir="ltr" />

			<nzlib-full-input asp-for="ViewModel.FirstName" />

			<nzlib-full-input asp-for="ViewModel.LastName" />

			<nzlib-full-input asp-for="ViewModel.CellPhoneNumber" dir="ltr" />

			<nzlib-full-checkbox asp-for="ViewModel.IsCellPhoneNumberVerified" />

			<nzlib-full-select asp-for="ViewModel.RoleId" asp-items="@(Model.RolesSelectList)" />

			<nzlib-full-checkbox asp-for="ViewModel.IsVisibleInContactUsPage" />

			<nzlib-full-input asp-for="ViewModel.TitleInContactUsPage" />

			<nzlib-full-input asp-for="ViewModel.Ordering" />

			<nzlib-full-checkbox asp-for="ViewModel.IsActive" />

			<nzlib-full-checkbox asp-for="ViewModel.IsProgrammer" />

			<nzlib-full-checkbox asp-for="ViewModel.IsProfilePublic" />

			<nzlib-full-textarea asp-for="ViewModel.Description" />

			<nzlib-full-textarea asp-for="ViewModel.AdminDescription" />
		</fieldset>

		<section-form-buttons>
			<button-save />
			<button-reset />
		</section-form-buttons>

		<section-form-footer>
			<a asp-page="Index">
				@(Html.NZLibGetLinkCaptionForList())
			</a>
			<a asp-page="Details" asp-route-id="@(Model.ViewModel.Id)">
				@(Html.NZLibGetLinkCaptionForDetails())
			</a>
			<a asp-page="Delete" asp-route-id="@(Model.ViewModel.Id)">
				@(Html.NZLibGetLinkCaptionForDelete())
			</a>
		</section-form-footer>
	</section-form>

</form>
