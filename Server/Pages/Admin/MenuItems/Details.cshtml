@page
@model Server.Pages.Admin.MenuItems.DetailsModel

@{
	@if (Model.ViewModel == null)
	{
		return;
	}

	var pageTitle = Resources.PageTitles.MenuItemDetails;
	ViewData[Constants.ViewDataKeyName.PageTitle] = pageTitle;

	if (System.Threading.Thread.CurrentThread.CurrentUICulture.TextInfo.IsRightToLeft)
	{
		Layout = "Layouts/Rtl/_Layout";
	}
	else
	{
		Layout = "Layouts/Ltr/_Layout";
	}
}

<section-form>
	<fieldset disabled>
		<section-form-header>
			@(pageTitle)
		</section-form-header>

		<partial name="PartialViews/_DisplayPageMessages" />

		<nzlib-readonly-input asp-for="ViewModel.Title" />

		<nzlib-readonly-input asp-for="ViewModel.ParentTitle" />

		<nzlib-readonly-input asp-for="ViewModel.Ordering" />

		<nzlib-readonly-input asp-for="ViewModel.Link" />

		<nzlib-readonly-input asp-for="ViewModel.Icon" />

		<nzlib-readonly-input asp-for="ViewModel.IconPosition" />

		<nzlib-readonly-checkbox asp-for="ViewModel.IsActive" />

		<nzlib-readonly-checkbox asp-for="ViewModel.IsPublic" />

		<nzlib-readonly-checkbox asp-for="ViewModel.IsUndeletable" />
	</fieldset>

	<section-form-footer>
		<a asp-page="Index">
			@(Html.NZLibGetLinkCaptionForList())
		</a>
		<a asp-page="Update" asp-route-id="@(Model.ViewModel.Id)">
			@(Html.NZLibGetLinkCaptionForUpdate())
		</a>
		<a asp-page="Delete" asp-route-id="@(Model.ViewModel.Id)">
			@(Html.NZLibGetLinkCaptionForDelete())
		</a>
	</section-form-footer>
</section-form>



